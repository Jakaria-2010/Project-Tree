{
    "version": "0.2.0",
    "configurations": [
        
        {
            "type": "editor-browser",
            "request": "launch",
            "name": "Launch Integrated Browser against localhost",
            "url": "http://localhost:7700",
            "webRoot": "${workspaceFolder}"
        }
    ]
}